# Overview
気軽にpostcssとsassするためのサンプル

# init
```
npm install
```

# usage
```
npm run dev
```

自動でブラウザが起動します。  
src配下のファイルを変更すると、  
destフォルダに変換したファイルが吐き出されます。  

本番では以下のコマンドでコンパイルしたファイルを使用します

```
npm run build
```